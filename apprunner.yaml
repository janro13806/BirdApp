version: 1.0
runtime: python311

build:
  command: |
    pip install --no-cache-dir -r requirements.txt

run:
  runtime-version: 3.11
  command: gunicorn service:app --bind 0.0.0.0:8080 --timeout 120 --workers 1

network:
  port: 8080
